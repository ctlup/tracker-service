# Copy this file to .env and add your Google key.
# docker compose up automatically reads this .env file.
#
# If left empty the system still works, city/country fields will just be null.
GOOGLE_API_KEY=
